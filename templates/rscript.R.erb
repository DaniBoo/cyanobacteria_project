library(ape)

testtree <- read.tree("<%= self.sequence %>_<%= self.tree_number %>.txt")

unrooted_tr <- unroot(testtree)

write.tree(unrooted_tr, file="<%= self.sequence %>_<%= self.tree_number %>_unrooted.txt")